# nginx-capture-multi
example of nginx-capture-multi

# build image
```sh
cd hello-world
go build
cd ..
docker-compose build
```

# run
```sh
dokcer-compose up -d
```

# test
```sh
curl localhost:8080
docker-compose logs
```
